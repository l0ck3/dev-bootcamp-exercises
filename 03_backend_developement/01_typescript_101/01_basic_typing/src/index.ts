const hello;
const foo;
const bar;
const sumResult;
const digits;
const isOpen;
const empty;

// Leave the line below for tests to work properly.
export { hello, foo, bar, sumResult, digits, isOpen, empty };
